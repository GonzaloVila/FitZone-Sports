# Copiar a .env.test (NO commitear .env.test)
# Apunta al contenedor de docker-compose.test.yml.
DATABASE_URL="postgresql://fitzone:fitzone@localhost:55432/fitzone_test?schema=public"
NODE_ENV="test"
PORT=3001
