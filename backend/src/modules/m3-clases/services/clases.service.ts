// SERVICIO - M3 Clases (RF-06..08)
// Reglas: ventana RF-07 (48hs antes, cancelar hasta 2hs antes).
// Lista de espera (RF-08) = Observer (ADR-05) en Unidad III.
// Recibe interfaz por constructor (DI); nunca PrismaService directo.